# 公標網 — 政府標案每日監控

客戶：某媒體代理商的媒體中心（已去識別化）。需求規格定案於 `docs/requirements.md`。

## Agent skills

### Issue tracker

Issues live as GitHub issues in `fredrick84823/gov-tender-monitor`, via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, using the default label strings. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` plus `docs/adr/` at the repo root. See `docs/agents/domain.md`.
